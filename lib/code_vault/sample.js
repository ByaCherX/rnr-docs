/* Copyright RedDevSoftware */
/*
function virtualFunction(src, code_reflect, content, content_snapshot) {
  this.src = src;
  this.code_reflect = code_reflect;
  this.content = content;
  this.content_snapshot = content_snapshot;}
var virtualVariable = new virtualFunction('v8::content-cxx', '0x0F7E1B9AA32', 'V8', '0x00000000BC9872419049172542198527917XXA_\\>reply');
console.log(virtualVariable);
*/
/*
function RdsValidator(index, contract) {
  this.index_price = index;
  this.contract_id = contract;
  this.value = index;
}

function ObjectCoin(index, contract, order) {
  RdsValidator.call(this, index, contract);
  this.category = 'coin';
  this.order_id = order;
}
function ObjectToken(index, contract, order) {
  RdsValidator.call(this, index, contract);
  this.category = 'token';
  this.order_id = order;
}

const Ada = new ObjectCoin('input_ada_price', '0x0000C', 'order_id');
const Ankr = new ObjectToken('input_ankr_price', '0x0000T', 'order_id');
let ObjectPair=[];
console.log(Ada);
console.log(Ankr);
*/

void function test(a,b) {
  let x = 20;
  summary=a*b;
  console.log('void func >>>');
  console.log(summary);
}
